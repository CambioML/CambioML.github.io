'use client';
import PlaygroundPageContainer from '@/app/components/playground/PlaygroundPageContainer';

const PlaygroundPage = () => {
  return <PlaygroundPageContainer production={true} />;
};

export default PlaygroundPage;
